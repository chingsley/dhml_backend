// export { default as Cloudinary } from './Cloudinary';

// export const throwError = (errorObj) => {
//   throw new Error(JSON.stringify(errorObj));
// };

// export const handleError = (methodName, error, req, res, next) => {
//   try {
//     const { status, error: err, ...rest } = JSON.parse(error.message);
//     return res.status(status).json({ error: err, ...rest });
//   } catch (e) {
//     return next(`${methodName}: ${error.message}`);
//   }
// };
