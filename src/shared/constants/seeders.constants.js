const MAX_STAFF_COUNT = 500;
const MAX_USER_COUNT = Math.floor(MAX_STAFF_COUNT / 2);
const MAX_PRINCIPALS_COUNT = 2000;

module.exports = {
  MAX_STAFF_COUNT,
  MAX_USER_COUNT,
  MAX_PRINCIPALS_COUNT,
};
